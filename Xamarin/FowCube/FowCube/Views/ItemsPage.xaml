<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FowCube.Views.ItemsPage"
             Title="{Binding Title}"
             x:Name="BrowseItemsPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="AddItem_Clicked" />
    </ContentPage.ToolbarItems>

    <StackLayout>
        <ListView x:Name="CardsListView"
                  ItemsSource="{Binding Cards}"
                  VerticalOptions="FillAndExpand"
                  HasUnevenRows="true"
                  RefreshCommand="{Binding LoadCardsCommand}"
                  IsPullToRefreshEnabled="true"
                  IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                  CachingStrategy="RecycleElement"
                  ItemSelected="OnItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid x:Name="CardItem" Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- 
                                Questo l'ho rimosso perché dava fastidio con il layout.
                                Quando capirù come usarlo allora lo rimetterò su.
                            -->
                            <!--<StackLayout Padding="10" Grid.Column="1" Grid.Row="1" Grid.RowSpan="2"></StackLayout>-->
                            <Label Text="{Binding Name}" d:Text="{Binding .}"
                                LineBreakMode="TailTruncation" Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="Medium"  Grid.Column="0" Grid.Row="0" />
                            
                            <Label Text="{Binding Description}" d:Text="Item descripton"
                                LineBreakMode="TailTruncation" Style="{DynamicResource ListItemDetailTextStyle}"
                                FontSize="Small"  Grid.Column="0" Grid.Row="1"/>

                            <Button Text="Delete" Grid.Column="1" Grid.Row="0" 
                                    BindingContext="{Binding Source={x:Reference CardsListView}, Path=BindingContext}" 
                                    Command="{Binding DeleteCardCommand}" 
                                    CommandParameter="{Binding Source={x:Reference CardItem}, Path=BindingContext}"/>

                            <Button Text="Edit" Grid.Column="1" Grid.Row="1"
                                    BindingContext="{Binding Source={x:Reference CardsListView}, Path=BindingContext}" 
                                    Command="{Binding EditCardCommand}" 
                                    CommandParameter="{Binding Source={x:Reference CardItem}, Path=BindingContext}"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
    
</ContentPage>